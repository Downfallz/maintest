# ---------------------------------------------------------------------------
# .NET
# ---------------------------------------------------------------------------
artifacts/
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.binlog
*.nupkg
*.snupkg
TestResults/
.sonarqube/
coverage/
*.coverage
*.coveragexml
*.trx
BenchmarkDotNet.Artifacts/
.dotnet/

# ---------------------------------------------------------------------------
# IDEs and editors
# ---------------------------------------------------------------------------
.vs/
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# ---------------------------------------------------------------------------
# OS
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------------
# AI tooling: keep shared config, ignore personal/local state
# ---------------------------------------------------------------------------
.claude/settings.local.json
.claude/*.local.*
.mcp.local.json
.cursor/
.aider*
.continue/
*.local.md

# ---------------------------------------------------------------------------
# Secrets and local env
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pfx
*.key
secrets.json

# Generated game content
data/dst/

# Learning artifacts (docs/learning/artifacts.md)
runs/
*.trace.json

# Python (learning/, managed with uv)
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml

# The content studio writes a document through a temporary file next to it.
*.json.tmp

# The default --out of `evaluate` and of `simulate`, run from the repository root. Anchored: unanchored,
# these match at any depth, and a policy is committed with the evaluation that earned it a place
# (models/README.md) under exactly that name -- ci-69 landed without one because of it.
/evaluation.json
/simulation.csv

# scratch space for scripts/sweep-weight.py
.sweep/
.sweep.lock

{
  "why": "The heuristic's rung on the 30-health content (ADR 0068), and the first to start from stun-first. The standings moved with the health: on the benchmark seeds, content 4ab506fa, stun-first beats greedy 0.886, search-4 0.950 and the built-in lookahead 0.785, pressure-floor beats greedy 0.874, and search-4, the bar every earlier rung was checked against, scores 0.485 against greedy. The ladder therefore climbs from the strongest set on this content, stun-first, rather than from a set that no longer beats the baseline. The panel is greedy, stun-first itself and pressure-floor, a candidate ranking last if it falls below the start against any: against stun-first the start scores the mirror's 0.5, so a candidate has to beat the set it came from to rank. Random is left out: every set beats it, and it costs a third of the budget in a game where a match now lasts about 15 rounds. The check, off that line and off the heuristic's reading, is the lookahead with its built-in weights. The budget is 10 rounds of 16, seed 0, as for the earlier heuristic rungs. What would make it a rung: a set that beats stun-first on seeds it never saw without falling below it against greedy, pressure-floor or the check.",
  "initial": "learning/weights/stun-first.json",
  "opponent": "greedy,heuristic:learning/weights/stun-first.json,heuristic:learning/weights/pressure-floor.json",
  "check_opponent": "lookahead",
  "kind": "heuristic",
  "iterations": 10,
  "population": 16,
  "seed": 0
}
